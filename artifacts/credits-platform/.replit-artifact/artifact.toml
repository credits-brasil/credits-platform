kind = "web"
previewPath = "/"
title = "Plataforma Credits"
version = "1.0.0"
id = "artifacts/credits-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19256

[services.development]
run = "pnpm --filter @workspace/credits-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/credits-platform", "run", "build" ]
publicDir = "artifacts/credits-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19256"
BASE_PATH = "/"
